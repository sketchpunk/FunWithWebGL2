
Fungi.App = {
	Scene:null
	/*
	Init(resData){
		if(!Fungi.Init("FungiCanvas")) return;
		
		Fungi.gl.fFitScreen(1,1).fClear();
		
		if(resData)
			var ResoucePromise = Loader.Prepare(resData).Start(true);
			Promise.all([resPromise]).then(()=>{ onReady(); });
		}
	}

	onReady(){
		
	}
	*/
};


Fungi.Scene = null;

Fungi.Loader = null;